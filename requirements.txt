# ---- Core ----
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
pydantic-settings>=2.0
# Stateful workflow orchestration; minor line pinned for graph API stability.
langgraph>=1.2,<1.3

# ---- LLM ----
# openai SDK used because Alibaba Cloud Model Studio offers an OpenAI-compatible API mode.
openai>=1.0

# ---- Retry / rate-limit handling ----
tenacity>=8.0

# ---- TTS + Video Generation (Alibaba Cloud Model Studio) ----
dashscope>=1.20

# ---- Media Processing ----
# Platform wheels bundle FFmpeg, avoiding a separate system install on Windows.
imageio-ffmpeg>=0.6

# ---- Testing ----
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
PyYAML>=6.0
